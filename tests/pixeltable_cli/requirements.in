# What the corpus project installs besides pixeltable itself, whose pin the test session generates into
# requirements.txt. Both the corpus database and the per-test ones install these, so they share one image.
spacy
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
mistune
