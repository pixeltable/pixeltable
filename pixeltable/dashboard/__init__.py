"""Catalog-facing helpers used by the daemon's /api/dashboard/* routes.

The dashboard UI itself is hosted by the pxt daemon (see pxt_cli/server). To launch it,
run `pxt dashboard start` from a terminal.
"""
