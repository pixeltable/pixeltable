---
title: "pixeltable.Table.update"
sidebarTitle: "update"
description: "Update rows in this table."
icon: "circle-m"
---
## ⚠️ Documentation Issues

<Warning>
- Examples section exists in docstring but was not parsed by docstring_parser
</Warning>

## Signature

```python
update(value_spec: 'dict[str, Any]', where: "Optional['exprs.Expr']" = None, cascade: 'bool' = True) -> 'UpdateStatus'
```

<a href="https://github.com/pixeltable/pixeltable/blob/main/pixeltable/catalog/table.py#L1362" target="_self">View source on GitHub</a>

## Parameters

<ParamField path="value_spec" type="any" required>
  a dictionary mapping column names to literal values or Pixeltable expressions.

</ParamField>

<ParamField path="where" type="any" >
  a predicate to filter rows to update.

</ParamField>

<ParamField path="cascade" type="any" >
  if True, also update all computed columns that transitively depend on the updated columns.

</ParamField>

## Returns

An \[`UpdateStatus`\]\[pixeltable.UpdateStatus\] object containing information about the update.


## Examples

```python
Set column `int_col` to 1 for all rows:

>>> tbl.update({'int_col': 1})

Set column `int_col` to 1 for all rows where `int_col` is 0:

>>> tbl.update({'int_col': 1}, where=tbl.int_col == 0)

Set `int_col` to the value of `other_int_col` + 1:

>>> tbl.update({'int_col': tbl.other_int_col + 1})

Increment `int_col` by 1 for all rows where `int_col` is 0:

>>> tbl.update({'int_col': tbl.int_col + 1}, where=tbl.int_col == 0)
```


---

<Note>
This method belongs to the [`Table`](../Table) class.
</Note>
