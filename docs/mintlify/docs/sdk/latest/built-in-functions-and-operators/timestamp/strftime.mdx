---
title: "pixeltable.functions.timestamp.strftime"
sidebarTitle: "strftime"
description: "Return a string representing the date and time, controlled by an explicit format string."
icon: "circle-u"
---
Equivalent to [`datetime.strftime()`](https://docs.python.org/3/library/datetime.html#datetime.datetime.strftime).


## Signature

```python
pixeltable.functions.timestamp.strftime(self: Timestamp, format: String) -> String
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="format" type="String" required>
  The format string to control the output. For a complete list of formatting directives, see [`strftime()` and `strptime()` Behavior](https://docs.python.org/3/library/datetime.html#strftime-and-strptime-behavior).


</ParamField>

