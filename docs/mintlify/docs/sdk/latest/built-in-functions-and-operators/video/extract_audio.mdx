---
title: "pixeltable.functions.video.extract_audio"
sidebarTitle: "extract_audio"
description: "Extract an audio stream from a video."
icon: "circle-u"
---
## ⚠️ Documentation Issues

<Warning>
- Examples section exists in docstring but was not parsed by docstring_parser
</Warning>

## Signature

```python
pixeltable.functions.video.extract_audio(video_path: Video, stream_idx: Int, format: String, codec: Optional[String]) -> Audio
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="stream_idx" type="Int" required>
  Index of the audio stream to extract.


</ParamField>

<ParamField path="format" type="String" required>
  The target audio format. (`'wav'`, `'mp3'`, `'flac'`).


</ParamField>

<ParamField path="codec" type="Optional[String]" required>
  The codec to use for the audio stream. If not provided, a default codec will be used.


</ParamField>

## Returns

<ResponseField name="return" type="any" required>
  The extracted audio.


</ResponseField>

## Examples

```python
Add a computed column to a table `tbl` that extracts audio from an existing column `video_col`:

>>> tbl.add_computed_column(
...     extracted_audio=tbl.video_col.extract_audio(format='flac')
... )
```

