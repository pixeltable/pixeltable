---
title: "pixeltable.functions.image.getpixel"
sidebarTitle: "getpixel"
description: "Return the pixel value at the given position. The position `xy` is a tuple containing the x and y coordinates."
icon: "circle-u"
---
Equivalent to [`PIL.Image.Image.getpixel()`](https://pillow.readthedocs.io/en/stable/reference/Image.html#PIL.Image.Image.getpixel)


## Signature

```python
pixeltable.functions.image.getpixel(self: Image, xy: Json) -> Json
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="xy" type="Json" required>
  The coordinates, given as (x, y).


</ParamField>

