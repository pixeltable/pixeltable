---
title: "pixeltable.functions.image.alpha_composite"
sidebarTitle: "alpha_composite"
description: "Alpha composite im2 over im1."
icon: "circle-u"
---
## Signature

```python
pixeltable.functions.image.alpha_composite(*args: 'Any', **kwargs: 'Any') -> "'exprs.FunctionCall'"
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="im1" type="string" required>
  The first image. Must have mode RGBA.


</ParamField>

<ParamField path="im2" type="string" required>
  The second image. Must have mode RGBA, and the same size as the first image.


</ParamField>

## Returns

<ResponseField name="return" type="any" required>
  An :py:class:`~PIL.Image.Image` object.


</ResponseField>

