---
title: "pixeltable.functions.image.getextrema"
sidebarTitle: "getextrema"
description: "Gets the minimum and maximum pixel values for each band in"
icon: "circle-u"
---
the image.


## Signature

```python
pixeltable.functions.image.getextrema(*args: 'Any', **kwargs: 'Any') -> "'exprs.FunctionCall'"
```

## Returns

<ResponseField name="return" type="any" required>
  For a single-band image, a 2-tuple containing the minimum and maximum pixel value. For a multi-band image, a tuple containing one 2-tuple for each band.


</ResponseField>

