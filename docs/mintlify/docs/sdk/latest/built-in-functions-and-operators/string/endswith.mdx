---
title: "pixeltable.functions.string.endswith"
sidebarTitle: "endswith"
description: "Return `True` if the string ends with the specified suffix, otherwise return `False`."
icon: "circle-u"
---
Equivalent to [`str.endswith()`](https://docs.python.org/3/library/stdtypes.html#str.endswith).


## Signature

```python
pixeltable.functions.string.endswith(*args: 'Any', **kwargs: 'Any') -> "'exprs.FunctionCall'"
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="substr" type="string" required>
  string literal


</ParamField>

