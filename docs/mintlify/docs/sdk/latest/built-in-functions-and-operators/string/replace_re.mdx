---
title: "pixeltable.functions.string.replace_re"
sidebarTitle: "replace_re"
description: "Replace occurrences of a regular expression pattern with `repl`."
icon: "circle-u"
---
Equivalent to [`re.sub()`](https://docs.python.org/3/library/re.html#re.sub).


## Signature

```python
pixeltable.functions.string.replace_re(self: String, pattern: String, repl: String, n: Optional[Int], flags: Int) -> String
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="pattern" type="String" required>
  regular expression pattern


</ParamField>

<ParamField path="repl" type="String" required>
  replacement string


</ParamField>

<ParamField path="n" type="Optional[Int]" required>
  number of replacements to make (if `None`, replace all occurrences)


</ParamField>

<ParamField path="flags" type="Int" required>
  [flags](https://docs.python.org/3/library/re.html#flags) for the `re` module


</ParamField>

