---
title: "pixeltable.functions.string.find"
sidebarTitle: "find"
description: "Return the lowest index in string where `substr` is found within the slice `s[start:end]`."
icon: "circle-u"
---
Equivalent to [`str.find()`](https://docs.python.org/3/library/stdtypes.html#str.find).


## Signature

```python
pixeltable.functions.string.find(*args: 'Any', **kwargs: 'Any') -> "'exprs.FunctionCall'"
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="substr" type="string" required>
  substring to search for


</ParamField>

<ParamField path="start" type="string" required>
  slice start


</ParamField>

<ParamField path="end" type="string" required>
  slice end


</ParamField>

