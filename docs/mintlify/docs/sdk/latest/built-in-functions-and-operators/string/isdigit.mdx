---
title: "pixeltable.functions.string.isdigit"
sidebarTitle: "isdigit"
description: "Return `True` if all characters in the string are digits and there is at least one character, `False` otherwise."
icon: "circle-u"
---
Equivalent to [`str.isdigit()`](https://docs.python.org/3/library/stdtypes.html#str.isdigit).


## Signature

```python
pixeltable.functions.string.isdigit(self: String) -> Bool
```

