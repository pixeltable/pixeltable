---
title: "pixeltable.functions.string.findall"
sidebarTitle: "findall"
description: "Find all occurrences of a regular expression pattern in string."
icon: "circle-u"
---
Equivalent to [`re.findall()`](https://docs.python.org/3/library/re.html#re.findall).


## Signature

```python
pixeltable.functions.string.findall(self: String, pattern: String, flags: Int) -> Json
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="pattern" type="String" required>
  regular expression pattern


</ParamField>

<ParamField path="flags" type="Int" required>
  [flags](https://docs.python.org/3/library/re.html#flags) for the `re` module


</ParamField>

