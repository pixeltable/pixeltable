---
title: "pixeltable.functions.string.partition"
sidebarTitle: "partition"
description: "Splits string at the first occurrence of `sep`, and returns 3 elements containing the part before the"
icon: "circle-u"
---
separator, the separator itself, and the part after the separator. If the separator is not found, return 3 elements containing string itself, followed by two empty strings.


## Signature

```python
pixeltable.functions.string.partition(self: String, sep: String) -> Json
```

