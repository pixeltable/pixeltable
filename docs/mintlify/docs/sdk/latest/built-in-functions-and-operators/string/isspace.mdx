---
title: "pixeltable.functions.string.isspace"
sidebarTitle: "isspace"
description: "Return `True` if there are only whitespace characters in the string and there is at least one character,"
icon: "circle-u"
---
`False` otherwise.

Equivalent to [`str.isspace()`](https://docs.python.org/3/library/stdtypes.html#str.isspace)


## Signature

```python
pixeltable.functions.string.isspace(self: String) -> Bool
```

