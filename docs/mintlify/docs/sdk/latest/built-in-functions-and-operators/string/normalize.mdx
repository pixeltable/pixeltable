---
title: "pixeltable.functions.string.normalize"
sidebarTitle: "normalize"
description: "Return the Unicode normal form."
icon: "circle-u"
---
Equivalent to [`unicodedata.normalize()`](https://docs.python.org/3/library/unicodedata.html#unicodedata.normalize)


## Signature

```python
pixeltable.functions.string.normalize(self: String, form: String) -> String
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="form" type="String" required>
  Unicode normal form (`'NFC'`, `'NFKC'`, `'NFD'`, `'NFKD'`)


</ParamField>

