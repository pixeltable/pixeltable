---
title: "pixeltable.ext.functions.yolox.yolo_to_coco"
sidebarTitle: "yolo_to_coco"
description: "Converts the output of a YOLOX object detection model to COCO format."
icon: "circle-u"
---
## ⚠️ Documentation Issues

<Warning>
- Examples section exists in docstring but was not parsed by docstring_parser
</Warning>

YOLOX is part of the `pixeltable.ext` package: long-term support in Pixeltable is not guaranteed.


## Signature

```python
pixeltable.ext.functions.yolox.yolo_to_coco(*args: 'Any', **kwargs: 'Any') -> "'exprs.FunctionCall'"
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="detections" type="string" required>
  The output of a YOLOX object detection model, as returned by `yolox`.


</ParamField>

## Returns

<ResponseField name="return" type="any" required>
  A dictionary containing the data from `detections`, converted to COCO format.


</ResponseField>

## Examples

```python
Add a computed column that converts the output `tbl.detections` to COCO format, where `tbl.image`
is the image for which detections were computed:

>>> tbl.add_computed_column(detections=yolox(tbl.image, model_id='yolox_m', threshold=0.8))
... tbl.add_computed_column(detections_coco=yolo_to_coco(tbl.detections))
```

