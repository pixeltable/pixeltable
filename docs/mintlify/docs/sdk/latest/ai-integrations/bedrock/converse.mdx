---
title: "pixeltable.functions.bedrock.converse"
sidebarTitle: "converse"
description: "Generate a conversation response."
icon: "circle-u"
---
## ⚠️ Documentation Issues

<Warning>
- Examples section exists in docstring but was not parsed by docstring_parser
</Warning>

Equivalent to the AWS Bedrock `converse` API endpoint. For additional details, see: https://docs.aws.amazon.com/bedrock/latest/userguide/conversation-inference.html

**Requirements:**

- `pip install boto3`


## Signature

```python
pixeltable.functions.bedrock.converse(*args: 'Any', **kwargs: 'Any') -> "'exprs.FunctionCall'"
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="messages" type="string" required>
  Input messages.


</ParamField>

<ParamField path="model_id" type="string" required>
  The model that will complete your prompt.


</ParamField>

<ParamField path="system" type="string" required>
  An optional system prompt.


</ParamField>

<ParamField path="inference_config" type="string" required>
  Base inference parameters to use.


</ParamField>

<ParamField path="additional_model_request_fields" type="string" required>
  Additional inference parameters to use.


</ParamField>

## Returns

<ResponseField name="return" type="any" required>
  A dictionary containing the response and other metadata.


</ResponseField>

## Examples

```python
Add a computed column that applies the model `anthropic.claude-3-haiku-20240307-v1:0`
to an existing Pixeltable column `tbl.prompt` of the table `tbl`:

>>> msgs = [{'role': 'user', 'content': [{'text': tbl.prompt}]}]
... tbl.add_computed_column(response=messages(msgs, model_id='anthropic.claude-3-haiku-20240307-v1:0'))
```

