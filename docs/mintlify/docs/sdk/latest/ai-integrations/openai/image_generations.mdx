---
title: "pixeltable.functions.openai.image_generations"
sidebarTitle: "image_generations"
description: "Creates an image given a prompt."
icon: "circle-u"
---
## ⚠️ Documentation Issues

<Warning>
- Examples section exists in docstring but was not parsed by docstring_parser
</Warning>

Equivalent to the OpenAI `images/generations` API endpoint. For additional details, see: https://platform.openai.com/docs/guides/images

Request throttling: Applies the rate limit set in the config (section `openai.rate_limits`; use the model id as the key). If no rate limit is configured, uses a default of 600 RPM.

**Requirements:**

- `pip install openai`


## Signature

```python
pixeltable.functions.openai.image_generations(prompt: String, model: String, model_kwargs: Optional[Json]) -> Image
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="prompt" type="String" required>
  Prompt for the image.


</ParamField>

<ParamField path="model" type="String" required>
  The model to use for the generations.


</ParamField>

<ParamField path="model_kwargs" type="Optional[Json]" required>
  Additional keyword args for the OpenAI `images/generations` API. For details on the available parameters, see: https://platform.openai.com/docs/api-reference/images/create


</ParamField>

## Returns

<ResponseField name="return" type="any" required>
  The generated image.


</ResponseField>

## Examples

```python
Add a computed column that applies the model `dall-e-2` to an existing
Pixeltable column `tbl.text` of the table `tbl`:

>>> tbl.add_computed_column(gen_image=image_generations(tbl.text, model='dall-e-2'))
```

