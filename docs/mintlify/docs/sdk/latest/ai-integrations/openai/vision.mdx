---
title: "pixeltable.functions.openai.vision"
sidebarTitle: "vision"
description: "Analyzes an image with the OpenAI vision capability. This is a convenience function that takes an image and"
icon: "circle-u"
---
## ⚠️ Documentation Issues

<Warning>
- Examples section exists in docstring but was not parsed by docstring_parser
</Warning>

prompt, and constructs a chat completion request that utilizes OpenAI vision.

For additional details, see: https://platform.openai.com/docs/guides/vision

Request throttling: Uses the rate limit-related headers returned by the API to throttle requests adaptively, based on available request and token capacity. No configuration is necessary.

**Requirements:**

- `pip install openai`


## Signature

```python
pixeltable.functions.openai.vision(prompt: String, image: Image, model: String, model_kwargs: Optional[Json]) -> String
```

## Args

<Note>
Parameter optional/required status may not be accurate if docstring doesn't specify defaults.
</Note>

<ParamField path="prompt" type="String" required>
  A prompt for the OpenAI vision request.


</ParamField>

<ParamField path="image" type="Image" required>
  The image to analyze.


</ParamField>

<ParamField path="model" type="String" required>
  The model to use for OpenAI vision.


</ParamField>

## Returns

<ResponseField name="return" type="any" required>
  The response from the OpenAI vision API.


</ResponseField>

## Examples

```python
Add a computed column that applies the model `gpt-4o-mini` to an existing Pixeltable column `tbl.image`
of the table `tbl`:

>>> tbl.add_computed_column(response=vision("What's in this image?", tbl.image, model='gpt-4o-mini'))
```

