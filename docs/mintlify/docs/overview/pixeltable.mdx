---
title: 'Overview'
description: 'Tired of choosing between months of infrastructure work or limited frameworks? Pixeltable gives you both speed and control. Pixeltable provide a declarative, incremental approach for multimodal AI workloads.'
icon: "table"
---

## Core Capabilities

<CardGroup cols={2}>
<Card 
  title="Multimodal Data Store" 
  icon="database"
  href="/docs/datastore/tables-and-operations"
>
  - Universal data ingestion (S3, URLs, local files)
  - Automatic versioning and lineage tracking
  - Efficient chunking and preprocessing
  - Rich query capabilities at any granularity
</Card>

<Card 
  title="AI Development Orchestration" 
  icon="brain-circuit"
  href="/docs/ai-apps/embedding-index"
>
  - Seamless model integration (OpenAI, HuggingFace)
  - Built-in vector similarity search
  - Automatic orchestration of AI workloads
  - Real-time inference and streaming
</Card>
</CardGroup>

## Quick Examples

<CodeGroup>
```python Multimodal Chat Interface
import pixeltable as pxt
from pixeltable.iterators import DocumentSplitter
from pixeltable.functions import openai, image

# Initialize conversation store
messages = pxt.create_table('chat.messages', {
    'session_id': pxt.String,
    'role': pxt.String,
    'content': pxt.String,
    'image': pxt.Image,  # Optional image attachment
    'timestamp': pxt.Timestamp
})

# Add computed columns for image analysis
messages['image_description'] = image.analyze(
    messages.image,
    model='gpt-4-vision'
)

# Create context management system
@messages.query
def get_context(session_id: str, limit: int = 5):
    return messages.where(
        messages.session_id == session_id
    ).order_by(
        messages.timestamp, asc=False
    ).limit(limit)

# Add response generation
messages['response'] = openai.chat_completions(
    model='gpt-4',
    messages=[
        {
            'role': 'system',
            'content': 'You are a helpful assistant.'
        },
        *get_context(messages.session_id)
    ]
).choices[0].message.content
```

```python Object detection in images using DETR model
import pixeltable as pxt
from pixeltable.functions import huggingface

# Create a table to store data persistently
t = pxt.create_table('image', {'image': pxt.Image})

# Insert some images
prefix = 'https://upload.wikimedia.org/wikipedia/commons'
paths = [
    '/1/15/Cat_August_2010-4.jpg',
    '/e/e1/Example_of_a_Dog.jpg',
    '/thumb/b/bf/Bird_Diversity_2013.png/300px-Bird_Diversity_2013.png'
]
t.insert({'image': prefix + p} for p in paths)

# Add a computed column for image classification
t.add_computed_column(classification=huggingface.detr_for_object_detection(
    t.image,
    model_id='facebook/detr-resnet-50'
))

# Retrieve the rows where cats have been identified
t.select(animal = t.image,
         classification = t.classification.label_text[0]) \
.where(t.classification.label_text[0]=='cat').head()
```

```python Text and Image Similarity Search
import pixeltable as pxt
from pixeltable.functions.huggingface import clip
from pixeltable.iterators import FrameIterator
import PIL.Image

video_table = pxt.create_table('videos', {'video': pxt.Video})

video_table.insert([{'video': '/video.mp4'}])

frames_view = pxt.create_view(
    'frames', video_table, iterator=FrameIterator.create(video=video_table.video))

# Create an index on the 'frame' column that allows text and image search
frames_view.add_embedding_index('frame', embed=clip.using('openai/clip-vit-base-patch32'))

# Now we will retrieve images based on a sample image
sample_image = '/image.jpeg'
sim = frames_view.frame.similarity(sample_image)
frames_view.order_by(sim, asc=False).limit(5).select(frames_view.frame, sim=sim).collect()

# Now we will retrieve images based on a string
sample_text = 'red truck'
sim = frames_view.frame.similarity(sample_text)
frames_view.order_by(sim, asc=False).limit(5).select(frames_view.frame, sim=sim).collect()
```
</CodeGroup>


## Application Domains

<CardGroup cols={2}>
<Card 
  title="Chat & LLM Systems" 
  icon="message"
  href="/examples/chat/overview"
>
  - Multimodal chat interfaces
  - Context-aware memory systems
  - Tool integration (web search, APIs)
  - RAG pipelines with auto-indexing
</Card>

<Card 
  title="Computer Vision Pipelines" 
  icon="camera"
  href="/examples/vision/overview"
>
  - Label Studio data pipelines
  - Real-time object detection 
  - Video processing & analysis
  - Dataset curation with Voxel51
</Card>

<Card 
  title="Search Applications" 
  icon="magnifying-glass"
  href="/examples/search/overview"
>
  - Audio content search
  - Visual similarity search
  - Document & web indexing
  - Cross-modal retrieval
</Card>

<Card 
  title="Evaluation Infrastructure" 
  icon="square-check"
  href="/examples/evaluation/overview"
>
  - LLM-based evaluation
  - Model comparison frameworks
  - Automated metrics pipelines
  - Performance monitoring
</Card>
</CardGroup>

## Get Started

<Steps>
  <Step title="Install Pixeltable" icon="download">
    ```bash
    pip install pixeltable
    ```
  </Step>
  
  <Step title="Choose Example" icon="list-check">
    Select a starter template from our examples gallery
  </Step>
  
  <Step title="Build Your App" icon="code">
    Follow our guides to customize for your needs
  </Step>
</Steps>

## Community & Support

<CardGroup cols={3}>
  <Card title="Join Discord" icon="discord" href="https://discord.gg/QPyqFYx2UN">
    Get help from our community
  </Card>
  <Card title="Read Docs" icon="book" href="/docs/overview/quick-start">
    Learn Pixeltable fundamentals
  </Card>
  <Card title="GitHub" icon="github" href="https://github.com/pixeltable/pixeltable">
    Star us and contribute
  </Card>
</CardGroup>