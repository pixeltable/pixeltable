---
title: 'Build Production AI Apps in Minutes'
description: 'Open-source declarative framework for multimodal AI applications'
---

<Tooltip >
  ```bash
  pip install pixeltable      
  ```
</Tooltip>

## Quick Example

<CodeGroup>
```python Quick Start
import pixeltable as pxt

# Create a video table
videos = pxt.create_table('videos', {'video': pxt.VideoType()})

# Automatic frame extraction
frames = pxt.create_view('frames', videos, 
    iterator=FrameIterator.create(video=videos.video))

# Add AI processing - only runs on new data
frames.add_computed_column(
    detect_objects=yolox(frames.frame, 
    model_id='yolox_tiny', 
    threshold=0.25)
)
```

```python RAG Example
# Create document table
docs = pxt.create_table('knowledge_base', 
    {'document': pxt.DocumentType()})

# Automatic chunking and embedding
chunks = pxt.create_view('chunks', docs,
    iterator=DocumentSplitter.create(
        document=docs.document,
        limit=300
    ))

chunks.add_embedding_index(
    'text',
    idx_name='minilm_idx',
    string_embed=sentence_transformer.using(
        model_id='sentence-transformers/all-MiniLM-L12-v2'
    )
)
```
</CodeGroup>

<AccordionGroup>
  <Accordion icon="sparkles" title="Declarative AI Workflows">
    Express complex operations through simple table operations:
    - Data transformations
    - Model inference
    - Custom logic
    - Multimodal data handling
  </Accordion>

  <Accordion icon="rocket" title="Built for Production">
    - Automatic versioning and lineage tracking
    - Incremental updates
    - Efficient storage and processing
    - Scale from development to production
  </Accordion>

  <Accordion icon="cube" title="Multimodal Support">
    Handle all data types seamlessly:
    - Text & Documents
    - Images & Video
    - Audio
    - Structured Data
  </Accordion>
</AccordionGroup>

## Core Features

<CardGroup cols={2}>
  <Card title="Computed Columns" icon="function">
    Add AI processing that automatically runs on new data
  </Card>
  <Card title="Versioning" icon="code-branch">
    Built-in versioning and lineage tracking
  </Card>
  <Card title="Incremental Updates" icon="arrows-rotate">
    Process only what's new or changed
  </Card>
  <Card title="Production Ready" icon="rocket">
    From prototype to production without rebuilding
  </Card>
</CardGroup>

## Popular Use Cases

<Tabs>
  <Tab title="RAG Systems">
    <Frame>
      - Automatic document processing
      - Flexible chunking strategies
      - Embedded vector search
      - Complete answer lineage
    </Frame>
  </Tab>
  <Tab title="Computer Vision">
    <Frame>
      - Automatic frame extraction
      - Object detection pipelines
      - Video processing
      - Annotation management
    </Frame>
  </Tab>
  <Tab title="Multimodal">
    <Frame>
      - Text-to-speech pipelines
      - Video transcription
      - Cross-modal search
      - Unified data management
    </Frame>
  </Tab>
</Tabs>

<Callout type="warning" emoji="⚡">
  Tired of choosing between months of infrastructure work or limited frameworks? Pixeltable gives you both speed and control.
</Callout>

## Next Steps

<Steps>
  <Step title="Installation">
    Run `pip install pixeltable` to get started
  </Step>
  <Step title="Quick Tutorial">
    Follow our [10-minute tutorial](/tutorials/quickstart)
  </Step>
  <Step title="Example Gallery">
    Explore [example applications](https://huggingface.co/pixeltable)
  </Step>
</Steps>

<Update>
  Subscribe to our [GitHub repository](https://github.com/pixeltable/pixeltable) for updates and new features.
</Update>