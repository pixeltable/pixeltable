---
title: 'Starter kit'
description: 'uvx pixeltable-new writes an application file. pxt schema update applies it. pxt service update serves it.'
---

The [Pixeltable Starter Kit](https://github.com/pixeltable/pixeltable-starter-kit) is two apps.
`uvx pixeltable-new` copies one into a new directory.

A first run: [Quickstart](/overview/quick-start).

```bash
uvx pixeltable-new myapp
cd myapp
uv sync
pxt schema update app.py agent
pxt service update app.py agent
```

`agent` is a catalog directory, not a folder on disk. Video: `uvx pixeltable-new myapp --video`, then TARGET `videointel`.
The scaffold includes `pixeltable.toml`. If you copied files by hand, run `pxt init` first.

Routes live on a `FastAPIRouter` in the same file as the `TableModel` classes.
Already have FastAPI: `app.include_router(api)`. [HTTP serving](/howto/deployment/serving).
No HTTP: [Self-hosting](/howto/deployment/overview).

## Apps

<Columns cols={2}>
  <Card title="Chat agent" icon="comments">
    Default. Knowledge insert needs no API key. `/ask` needs `ANTHROPIC_API_KEY`.

    ```bash
    uvx pixeltable-new myapp
    pxt schema update app.py agent
    ```
  </Card>
  <Card title="Video search" icon="film">
    Frames at 1 FPS, CLIP search, image ingest.

    ```bash
    uvx pixeltable-new myapp --video
    pxt schema update app.py videointel
    ```
  </Card>
</Columns>

Cloud reads
[`gallery.json`](https://github.com/pixeltable/pixeltable-starter-kit/blob/main/gallery.json).
Same file against Cloud: `pxt schema update app.py pxt://org:mydb`.
[Deploy to Pixeltable Cloud](/howto/deployment/cloud). `pxt service` stays local.

<CardGroup cols={2}>
  <Card title="AI Skills" icon="robot" href="/resources/ai-skills">
    Teach an agent to write `TableModel` in `app.py`.
  </Card>
  <Card title="Clone the kit" icon="github" href="https://github.com/pixeltable/pixeltable-starter-kit">
    `chat-agent/` and `video-search/` in one repo.
  </Card>
</CardGroup>
