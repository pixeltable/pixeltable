---
title: 'Python SDK'
sidebarTitle: 'Python SDK'
description: 'Pixeltable Python SDK reference'
---

export const Redirect = ({ to }) => {
  if (typeof window !== 'undefined') {
    window.location.replace(to);
  }
  return null;
};

<Redirect to="/sdk/latest/pixeltable" />

[Python SDK reference](/sdk/latest/pixeltable)
