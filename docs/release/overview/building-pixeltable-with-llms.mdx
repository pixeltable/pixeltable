---
title: 'AI coding agents'
description: 'Install the Pixeltable skill, then use the same application file locally and on Pixeltable Cloud'
---

Hand the agent [get-started.md](https://www.pixeltable.com/get-started.md) to install the CLI, the [skill](https://github.com/pixeltable/pixeltable-skill), and MCP. After that, follow the [Quickstart](/overview/quick-start): generate an example app, create its tables, then start the endpoints.

The skill tells the coding agent not to use LangChain, pandas as a database, per-row model loops, or a separate vector database. An application is a Python file that declares tables as `TableModel` classes. Notebooks can still use `pxt.create_table()`.

## Set Up Your AI Tool

```bash
npx skills add pixeltable/pixeltable-skill
```

Works with tools that support [Agent Skills](https://agentskills.io/specification): Cursor, Claude Code, Windsurf, Cline, OpenCode, Codex CLI, and others.

<Tabs>
  <Tab title="Cursor / Windsurf">
    To install the skill, run `npx skills add`. Do not copy this repository's `AGENTS.md` into an application; that file is for Pixeltable contributors.
  </Tab>
  <Tab title="Claude Code">
    Same `npx skills add`, or the plugin:

    ```bash
    /plugin marketplace add pixeltable/pixeltable-skill
    /plugin install pixeltable-skill@pixeltable-skill
    ```
  </Tab>
  <Tab title="Any LLM">
    If your tool has no skill support, append `.md` to any docs URL and paste that markdown into the chat. Site index: [llms.txt](https://docs.pixeltable.com/llms.txt) ([standard](https://llmstxt.org/)). Capability summary: [skill.md](https://docs.pixeltable.com/skill.md).

    | Resource | URL |
    |----------|-----|
    | This page as markdown | [building-pixeltable-with-llms.md](https://docs.pixeltable.com/overview/building-pixeltable-with-llms.md) |
    | Site index | [llms.txt](https://docs.pixeltable.com/llms.txt) |
    | Full map | [llms-full.txt](https://docs.pixeltable.com/llms-full.txt) |
    | Capability summary | [skill.md](https://docs.pixeltable.com/skill.md) |
  </Tab>
</Tabs>

## What the agent should write

Write an application file, then create its tables with `pxt schema update app.py my_app`. When you insert a row, computed columns run automatically.

```python
import pixeltable as pxt
import pixeltable.functions as pxtf

TableModel = pxt.model_base()


class Articles(TableModel, name='articles'):
    title: pxt.String
    body: pxt.String
    response = pxtf.openai.chat_completions(
        messages=[{'role': 'user', 'content': body}],
        model='gpt-4o-mini',
    )
    summary = response.choices[0].message.content
```

```bash
pxt schema update app.py my_app
```

After the schema exists, open the table with `t = pxt.get_table('my_app.articles')` and insert rows, or serve the same file over [HTTP](/howto/deployment/serving). To run that file on Pixeltable Cloud, pass a `pxt://org:db` URI instead of `my_app`: [Deploy to Pixeltable Cloud](/howto/deployment/cloud).

## Docs search and catalog CLI

Hosted docs MCP for the editor:

```
https://docs.pixeltable.com/mcp
```

Catalog inspection without importing Python: `pxt ls --json`, `pxt describe --json`, `pxt errors`. The `pxt` CLI (v0.7.4+) exposes `--json` on inspection and query commands. See the [CLI](/platform/cli).

To call remote MCP tools from a table column, use `pxt.mcp_udfs(url)` (see [Agents as tables](/use-cases/agentic-workflows)).

Experimental local catalog MCP: [mcp-server-pixeltable-developer](https://github.com/pixeltable/mcp-server-pixeltable-developer).

## Next

<CardGroup cols={2}>
  <Card title="Quickstart" icon="bolt" href="/overview/quick-start">
    Write `app.py`, create the tables, then start the endpoints.
  </Card>
  <Card title="Agents as tables" icon="robot" href="/use-cases/agentic-workflows">
    Register tools and run `invoke_tools` as computed columns.
  </Card>
  <Card title="Starter kit" icon="github" href="https://github.com/pixeltable/pixeltable-starter-kit">
    Templates for serving, batch, and FastAPI + React.
  </Card>
  <Card title="Cloud" icon="cloud" href="/howto/deployment/cloud">
    Deploy the same `app.py` to a hosted database at `pxt://org:db`.
  </Card>
</CardGroup>
