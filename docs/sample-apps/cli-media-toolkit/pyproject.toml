[project]
name = "cli-media-toolkit"
version = "0.1.0"
description = "CLI media processing toolkit powered by Pixeltable"
requires-python = ">=3.11"
dependencies = [
    "pixeltable>=0.7.4",
    "pytubefix>=9.1",
    "click>=8.0",
    "openai-whisper",
    "transformers",
]
