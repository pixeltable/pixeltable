[project]
name = "trading-insight-server"
version = "0.1.0"
description = "FastAPI backend for AI trading chart analysis with Pixeltable"
requires-python = ">=3.11"
dependencies = [
    "pixeltable>=0.7.4",
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "pillow",
    "anthropic",
]
