[project]
name = "jfk-files-mcp-server"
version = "0.1.0"
description = "MCP server for JFK declassified files with semantic search"
requires-python = ">=3.11"
dependencies = [
    "pixeltable>=0.7.4",
    "beautifulsoup4",
    "httpx",
    "mistralai",
    "mcp",
    "python-dotenv",
    "requests",
    "sentence-transformers",
    "spacy",
    "starlette",
    "tiktoken",
    "uvicorn",
]

[project.optional-dependencies]
dev = ["ruff"]
