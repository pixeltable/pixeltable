[project]
name = "multimodal-chat-backend"
version = "0.1.0"
description = "FastAPI backend for multimodal RAG chat with Pixeltable"
requires-python = ">=3.11"
dependencies = [
    "pixeltable>=0.7.4",
    "fastapi[standard]",
    "uvicorn",
    "python-dotenv",
    "python-multipart",
    "openai",
    "sentence-transformers",
    "spacy",
    "pandas",
]
